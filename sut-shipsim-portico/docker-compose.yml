version: '3'
# ==============================================================================
# SHIPSIM SUT composition for the Portico RTI
# ==============================================================================

services:
  shipsim:
    image: app-docker136.hex.tno.nl/msaas-aus/shipsim:po
    network_mode: host
    environment:
    - FEDERATENAME=VRF/UG
