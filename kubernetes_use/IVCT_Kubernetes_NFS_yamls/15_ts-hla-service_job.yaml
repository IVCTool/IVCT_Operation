apiVersion: batch/v1
kind: Job
metadata:
  name: ts-hla-services
  # namespace: brf-test-namespace
spec:
  template:
    spec:
      containers:
      - args:
        - -r
        - /root/conf/TestSuites/TS_HLA_Services-2.1.2-SNAPSHOT
        - /runtimeconfig/TestSuites
        command:
        - cp
        image: ivct/ts-hla-services:2.1.2-SNAPSHOT
        imagePullPolicy: Always
        name: ts-hla-services
        volumeMounts:
        - mountPath: /runtimeconfig
          name: vol0
      restartPolicy: Never
      volumes:
      - name: vol0
        persistentVolumeClaim:
          claimName: nfs-pv-claim
